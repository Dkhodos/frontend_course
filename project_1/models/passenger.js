class Passenger {
    constructor(name, passportNumber) {
        this.name = name; // Passenger's name
        this.passportNumber = passportNumber; // Passport number
    }
}

export default Passenger;
