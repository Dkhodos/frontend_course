class Passenger {
  /**
   * Creates a new Passenger instance.
   *
   * @param {string} name - The name of the passenger.
   * @param {string} passportNumber - The passport number of the passenger.
   */
  constructor(name, passportNumber) {
    this.name = name;
    this.passportNumber = passportNumber;
  }
}

export default Passenger;
